---
id: test
title: "🌀 Test"
hide_title: false
hide_table_of_contents: false
sidebar_position: 20
image: /img/png/theme/z/320x320.png
description: Annex - Test documentation
keywords:
  - annex
  - test
draft: true
---

{/* @format */}

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import Link from "@docusaurus/Link";

An annex runs `zunit` and `make` tests if they are configured in the repository.

<video
  autoPlay
  loop
  muted
  playsInline
  width={863}
  height={703}
  style={{width: "100%", height: "auto", borderRadius: "var(--ifm-global-radius)"}}
>
  <source src="/img/cast/webm/annex/test-annex.webm" type="video/webm" />
  <img src="/img/plugins/annex/test-annex.gif" alt="Annex - z-a-test preview" loading="lazy" width={863} height={703} />
</video>

Simply load it like any other plugin to make it active:

```shell
zi light z-shell/z-a-test
```

<details>
  <summary>📖 <b>Configuration</b></summary>

To run the tests in a verbose mode, issue:

```shell
zstyle :zi:annex:test quiet 0
```

To skip tests for a single plugin before installing or updating add the `notest` ice-modifier:

```shell showLineNumbers
zi ice notest
zi load …
```

</details>
