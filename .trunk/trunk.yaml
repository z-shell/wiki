# @format

version: 0.1
cli:
  version: 0.16.1-beta
repo:
  repo:
    host: github.com
    owner: z-shell
    name: wiki
lint:
  disabled:
    - shellcheck
    - hadolint
    - taplo
    - svgo
  enabled:
    - stylelint@14.11.0
    - git-diff-check@SYSTEM
    - actionlint@1.6.16
    - eslint@8.22.0
    - gitleaks@8.11.0
    - markdownlint@0.32.2
    - prettier@2.7.1
    - shfmt@3.5.0
  ignore:
    - linters: [ALL]
      paths:
        - "i18n/**"
        - "build/**"
        - "static/**"
        - ".docusaurus/**"
    - linters: [gitleaks]
      paths:
        - "pnpm-lock.yaml"
        - "docusaurus.config.js"
