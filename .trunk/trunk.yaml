version: 0.1
cli:
  version: 0.12.1-beta
repo:
  repo:
    host: github.com
    owner: z-shell
    name: zw
lint:
  disabled:
    - hadolint
    - shellcheck
  linters:
    - name: markdownlint
      command: [markdownlint, -q, --config, .github/.markdownlint.yaml, '${target}']
      direct_configs: [.github/.markdownlint.yaml]
  enabled:
    - actionlint@1.6.13
    - eslint@8.16.0
    - gitleaks@8.8.7
    - markdownlint@0.31.1
    - prettier@2.6.2
    - shfmt@3.4.0
    - stylelint@14.8.5
    - stylelint-fmt@14.8.5
    - taplo-fmt@release-cli-0.6.3
    - taplo@release-cli-0.6.3
  ignore:
    - linters: [ALL]
      paths:
        - 'i18n/**'
        - '!18n/en/**'
        - 'build/**'
        - 'static/img/**'
        - 'static/assets/**'
        - '.docusaurus/**'
    - linters: [gitleaks]
      paths:
        - 'pnpm-lock.yaml'
