version: 0.1
cli:
  version: 0.11.0-beta
repo:
  repo:
    host: github.com
    owner: z-shell
    name: zw
lint:
  linters:
    - name: markdownlint
      command: [markdownlint, -q, --config, .trunk/.markdownlint.yaml, '${target}']
      direct_configs: [.trunk/.markdownlint.yaml]
  enabled:
    - actionlint@1.6.12
    - gitleaks@8.8.4
    - markdownlint@0.31.1
    - prettier@2.6.2
    - shfmt@3.4.0
  ignore:
    - linters: [ALL]
      paths:
        - 'i18n/**'
        - '!18n/en/**'
        - 'build/**'
        - 'static/**'
        - '.docusaurus/**'
        - 'src/css/fa6.1.1/**'
    - linters: [actionlint]
      paths:
        - '.github/workflows/sync-labels.yml'
#    - linters: [gitleaks]
#      paths:
#        - 'src/components/AsciinemaPlayer/asciinema-player.js'
#        - 'src/components/AsciinemaPlayer/asciinema-player.min.js'
