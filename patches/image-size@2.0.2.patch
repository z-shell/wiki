diff --git a/dist/detector.cjs b/dist/detector.cjs
index 0898608dbeca36722dfd795341b1c3c1448f58aa..302e375ee4d3ddb22ab618e633f8b5fe013fc666 100644
--- a/dist/detector.cjs
+++ b/dist/detector.cjs
@@ -171,7 +171,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -253,6 +253,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -488,7 +489,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/detector.mjs b/dist/detector.mjs
index 67afcab02d627f95e98c938499e07a934b832a26..99cd0974ca43d4ac6af7952a6d5ac04dc7304964 100644
--- a/dist/detector.mjs
+++ b/dist/detector.mjs
@@ -169,7 +169,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -251,6 +251,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -486,7 +487,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/fromFile.cjs b/dist/fromFile.cjs
index c226d3dac07f235db456c774b26cca88f655e1af..a5f5bb45754c0094c4d23987b115ef068ea15fe9 100644
--- a/dist/fromFile.cjs
+++ b/dist/fromFile.cjs
@@ -197,7 +197,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -279,6 +279,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -514,7 +515,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/fromFile.mjs b/dist/fromFile.mjs
index 4a3c44432982397204f7a9d04fc66ef65a8e7ba4..a02f25ddfa4b7112cc1416e95a74e4ad6052c22d 100644
--- a/dist/fromFile.mjs
+++ b/dist/fromFile.mjs
@@ -174,7 +174,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -256,6 +256,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -491,7 +492,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/index.cjs b/dist/index.cjs
index bde0210eb131db06ccca30a5c466c7252a03af50..b1fdf84d7d4c7420a6f1fa619bf4d50cbff2dd8a 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -173,7 +173,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -255,6 +255,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -490,7 +491,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/index.mjs b/dist/index.mjs
index 22c17afad4e406eba013fff7858366cb5f8e3ba4..3fe3082ab7f6950a92adc35b05a2cf5cc2d4607c 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -169,7 +169,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -251,6 +251,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -486,7 +487,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/lookup.cjs b/dist/lookup.cjs
index 9731ad8bb0949f8fdf9297851bdb312e91282f01..0967d0a26aa94dc09b4678adf8e62ae72be13a17 100644
--- a/dist/lookup.cjs
+++ b/dist/lookup.cjs
@@ -171,7 +171,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -253,6 +253,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -488,7 +489,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/lookup.mjs b/dist/lookup.mjs
index f787d1c8408d7b681fec8617595d058f66959869..86af71ebb188f2598c737c8e14b032883907e7c2 100644
--- a/dist/lookup.mjs
+++ b/dist/lookup.mjs
@@ -169,7 +169,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -251,6 +251,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize2 = getImageSize2(imageHeader[0]);
       images.push(imageSize2);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -486,7 +487,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/types/heif.cjs b/dist/types/heif.cjs
index 1f490788ea654ea9896060a5c0253d81e348af92..cb49c5648afe835964978e3cf53b98c5883ef6ed 100644
--- a/dist/types/heif.cjs
+++ b/dist/types/heif.cjs
@@ -69,7 +69,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
diff --git a/dist/types/heif.mjs b/dist/types/heif.mjs
index 01330919e01b3c034a96a7b4b1dbf5a7f658bb2d..57f4d1faf7c2365757c833f7fde3cd5729efde48 100644
--- a/dist/types/heif.mjs
+++ b/dist/types/heif.mjs
@@ -67,7 +67,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
diff --git a/dist/types/icns.cjs b/dist/types/icns.cjs
index 32c8d370dcf80a10f14a6deebfdbe04d3c70585c..82312048a56d73a123f80ec5300776c2b5c7f3df 100644
--- a/dist/types/icns.cjs
+++ b/dist/types/icns.cjs
@@ -74,6 +74,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
diff --git a/dist/types/icns.mjs b/dist/types/icns.mjs
index 8710027bc5890a79d05a7fdb87de3018e556729e..079605dc6cda24b567d8eb9a2ab80d99173311d6 100644
--- a/dist/types/icns.mjs
+++ b/dist/types/icns.mjs
@@ -72,6 +72,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
diff --git a/dist/types/index.cjs b/dist/types/index.cjs
index 6949cd134db7ab59dae7b804b36c7bacb4e387cf..e0bc6815b22518f64c5ccfc23038e718f0206ed4 100644
--- a/dist/types/index.cjs
+++ b/dist/types/index.cjs
@@ -171,7 +171,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -253,6 +253,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -488,7 +489,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/types/index.mjs b/dist/types/index.mjs
index b026dda4aa16c6077a76e4b3cfe3f54fe1d6d1d0..c009b2223b7b3b0e55287330bc0086680033cde1 100644
--- a/dist/types/index.mjs
+++ b/dist/types/index.mjs
@@ -169,7 +169,7 @@ var HEIF = {
         width = rawWidth - cropRight;
       }
       images.push({ height, width });
-      currentOffset = ispeBox.offset + ispeBox.size;
+      currentOffset = ispeBox.offset + (ispeBox.size > 0 ? ispeBox.size : 8);
     }
     if (images.length === 0) {
       throw new TypeError("Invalid HEIF, no sizes found");
@@ -251,6 +251,7 @@ var ICNS = {
       const imageHeader = readImageHeader(input, imageOffset);
       const imageSize = getImageSize2(imageHeader[0]);
       images.push(imageSize);
+      if (imageHeader[1] <= 0) return;
       imageOffset += imageHeader[1];
     }
     if (images.length === 0) {
@@ -486,7 +487,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/types/jxl.cjs b/dist/types/jxl.cjs
index a12095be85c7a8fd25763895a47c46b4292ebd9c..93d5f01aa4362808248d5492789a2fc3b3ca60a4 100644
--- a/dist/types/jxl.cjs
+++ b/dist/types/jxl.cjs
@@ -119,7 +119,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
diff --git a/dist/types/jxl.mjs b/dist/types/jxl.mjs
index 49b0c4cac6abb366378e5feed745f16981b441c0..24daee333740e2b80149165869a5033b99da8592 100644
--- a/dist/types/jxl.mjs
+++ b/dist/types/jxl.mjs
@@ -117,7 +117,7 @@ function extractPartialStreams(input) {
     partialStreams.push(
       input.slice(jxlpBox.offset + 12, jxlpBox.offset + jxlpBox.size)
     );
-    offset = jxlpBox.offset + jxlpBox.size;
+    offset = jxlpBox.offset + (jxlpBox.size > 0 ? jxlpBox.size : 8);
   }
   return partialStreams;
 }
